# Copy this file to ~/watchtower.env on the host, fill in a real token, then
# `chmod 600 ~/watchtower.env`. Never commit a real value.
#
# Generate a token with: openssl rand -hex 32
WATCHTOWER_HTTP_API_TOKEN=
